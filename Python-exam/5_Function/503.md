# 503 累加計算

## プログラム説明：

以下の条件を満たすプログラムを作成せよ。

ユーザーに2つの整数を入力させ、関数「compute()」を呼び出すこと。 関数「compute()」は引数「a」と「b」を受けることができ、返り値として「a」から「b」まで（「b」を含む）の全ての整数の和に設定すること。


## 入出力：

入力説明:

2つの整数

出力説明:

「a」～「b」累加の和

## 入出力例:

```
入力例
33
66
出力例
1683
```

